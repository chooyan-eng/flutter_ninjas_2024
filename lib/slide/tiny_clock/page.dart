import 'package:flutter/material.dart';

class TinyClockPage extends StatelessWidget {
  const TinyClockPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Case Study: TinyClock'),
      ),
    );
  }
}
